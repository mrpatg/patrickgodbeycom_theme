name: Theme Update

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: Requesting theme update
      run: curl --user ${{ secrets.auth }} ${{ secrets.update_webhook }}
